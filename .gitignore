# go.work is per-developer; committing one would leak into consumers' builds
go.work
go.work.sum

# build artifacts
*.exe
*.test
*.out

# editor/OS cruft
.DS_Store
.idea/
.vscode/
sloplint
.cccheck/

# local build artifacts from `go build` in the tools modules
tools/sloplint/sloplint
tools/coverdiff/coverdiff
